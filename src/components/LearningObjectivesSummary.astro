---
/**
 * Learning Objectives Summary component that automatically scans page content
 * and displays a preview of all learning objectives.
 */
---

<div class="objectives-summary-container">
  <h3 class="objectives-summary-title">Learning Objectives</h3>
  <p class="objectives-summary-intro">After completing this tutorial, you will be able to…</p>
  <div id="objectives-summary-list">
    <!-- Learning objectives will be populated here by JavaScript -->
  </div>
</div>

<style>
  .objectives-summary-container {
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--sl-color-hairline);
    display: flex;
    flex-direction: column;
    height: 100%; /* Take full height from parent flex container */
  }
  
  .objectives-summary-title {
    margin: 0 0 0.5rem 0;
    font-size: var(--sl-text-sm);
    font-weight: 600;
    color: var(--sl-color-white);
  }
  
  .objectives-summary-intro {
    margin: 0 0 0.75rem 0;
    font-size: var(--sl-text-xs);
    color: var(--sl-color-gray-2);
    line-height: 1.3;
  }
  
  #objectives-summary-list {
    margin: 0;
    padding: 0;
    overflow-y: auto;
    flex: 1;
  }
  
  /* Global styles for dynamically generated content */
  :global(#objectives-summary-list .objective-item) {
    margin-bottom: 0.75rem;
    cursor: pointer;
    transition: color 0.2s ease;
    border-radius: 0.25rem;
    padding: 0.25rem;
    margin-left: -0.25rem;
    margin-right: -0.25rem;
  }

  :global(#objectives-summary-list .objective-item:hover) {
    background-color: var(--sl-color-gray-6);
  }

  /* Dark mode hover */
  :global([data-theme="dark"] #objectives-summary-list .objective-item:hover) {
    background-color: var(--sl-color-gray-5);
  }
  
  :global(#objectives-summary-list .objective-item:last-child) {
    margin-bottom: 0;
  }
  
  :global(#objectives-summary-list .objective-label) {
    font-weight: 600;
    font-size: var(--sl-text-xs);
    color: var(--sl-color-gray-2);
    display: block;
    margin-bottom: 0.25rem;
    transition: color 0.2s ease;
  }
  
  :global(#objectives-summary-list .objective-text) {
    font-size: var(--sl-text-xs);
    line-height: 1.3;
    color: var(--sl-color-gray-2);
    display: block;
    white-space: normal;
    word-wrap: break-word;
    overflow-wrap: break-word;
    transition: color 0.2s ease;
  }
  
  /* Completed objective styling */
  :global(#objectives-summary-list .objective-item.completed .objective-label) {
    color: var(--color-green-600); /* Darker green for light mode */
  }
  
  :global(#objectives-summary-list .objective-item.completed .objective-text) {
    color: var(--color-green-600); /* Darker green for light mode */
  }
  
  /* Dark mode completed styling */
  :global([data-theme="dark"] #objectives-summary-list .objective-item.completed .objective-label) {
    color: var(--color-green-400); /* Lighter green for dark mode */
  }
  
  :global([data-theme="dark"] #objectives-summary-list .objective-item.completed .objective-text) {
    color: var(--color-green-400); /* Lighter green for dark mode */
  }
</style>